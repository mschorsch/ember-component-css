import Component from '@ember/component';
import Template from './template';
import {layout} from '@ember-decorators/component'

@layout(Template)
export default class BaseRules extends Component {
}
